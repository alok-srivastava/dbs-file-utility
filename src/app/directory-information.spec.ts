import { DirectoryInformation } from './directory-information';

describe('DirectoryInformation', () => {
  it('should create an instance', () => {
    expect(new DirectoryInformation()).toBeTruthy();
  });
});
